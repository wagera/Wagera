<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.astraupscale.app"
    android:versionCode="13"
    android:versionName="11.0">

    <!-- Surum denetimi icin; goruntu isleme internet kullanmaz. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Ekran kapaliyken islemin surmesi icin -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Bekleyen bildirimlerin yeniden baslatmadan sonra da gonderilmesi icin -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Galeriden fotograf secme. Fotograflar yalnizca cihazda okunur;
         hicbiri disariya gonderilmez. Izin verilmezse uygulama yine
         calisir, secim sistem belge secicisine duser. -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Galeriden video secme; fotografla ayni kural: hicbir kare disari cikmaz. -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-feature android:name="android.hardware.vulkan.version" android:required="false" />

    <application
        android:allowBackup="true"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            Acilis temasiyla baslar; MainActivity.onCreate ilk isi olarak
            setTheme(R.style.AppTheme) cagirir. Boylece pencere olusur
            olusmaz isaret ekranda olur ve ayri bir acilis Activity'sinin
            getirecegi ek kare odenmez.
        -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <!-- Kuyruktaki bildirimleri ag geldiginde gonderir -->
        <service
            android:name=".ReportSender$Job"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service
            android:name=".UpscaleService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
            Video isi ayri servistir: bir fotograf dakikalar, bir video
            saatler surer ve ikisi ayni anda calisabilmelidir.
        -->
        <service
            android:name=".VideoService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
